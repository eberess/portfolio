services:
  portfolio:
    build:
      context: .
      dockerfile: src/Dockerfile
    
    container_name: nextjs-portfolio
    
    ports:
      - '${NODE_APP_PORT}:3000'
        
    environment:
      NODE_ENV: production

    restart: always
